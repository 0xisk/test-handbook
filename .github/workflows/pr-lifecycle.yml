name: PR Lifecycle

on:
  pull_request:
    types: [opened, reopened, converted_to_draft, ready_for_review, closed]

permissions: {}

jobs:
  pr-lifecycle:
    uses: 0xisk/test-handbook/.github/workflows/pr-lifecycle-template.yml@main
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
